export const READER_UI = {
  TOP_TOOLBAR_HEIGHT_PX: 48,
  READING_AREA_PADDING_TOP_PX: 18,
  BOTTOM_NAV_HEIGHT_PX: 82,
  SAFE_AREA_TOP: 'clamp(20px, env(safe-area-inset-top, 20px), 44px)',
  SAFE_AREA_BOTTOM: 'clamp(10px, env(safe-area-inset-bottom, 10px), 34px)',
} as const;

